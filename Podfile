# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Fox Weather' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Fox Weather
  
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'SDWebImage'

  target 'Fox WeatherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Fox WeatherUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
